apiVersion: v1
kind: Service
metadata:
  name: {{ include "todonginx.fullname" . }}
  labels:
    {{- include "todonginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todonginx.selectorLabels" . | nindent 4 }}

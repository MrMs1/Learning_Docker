apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todonginx.fullname" . }}-test-connection"
  labels:
    {{- include "todonginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todonginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

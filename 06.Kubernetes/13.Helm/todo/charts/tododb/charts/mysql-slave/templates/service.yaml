apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-slave.fullname" . }}
  labels:
    {{- include "mysql-slave.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mysql-slave.selectorLabels" . | nindent 4 }}
